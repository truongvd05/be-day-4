## Environment Setup

Create a `.env` file based on `.env.example` and update the values.

API Endpoints
/api/posts

`example`
`lấy trang 1`
/api/posts?page=1

`lấy ra danh sách bài post của user_id 1`
/api/posts?user_id=1

`lấy trang 1 của danh sách bài post user_id 1`
/api/posts?page=1&user_id=1
